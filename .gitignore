# DelugePatchBook is the shipped application: index.html, app.js, styles.css,
# modules/, README.md, LICENSE. Everything below is development/test tooling
# that lives alongside it but isn't part of the app itself -- see tests/
# own package.json (it's a separate, dependency-having project on purpose).

# The entire dev/QA project stays out of the (public) repo entirely: spec
# files, fixtures (some of which are real device/preset dumps not meant for
# public redistribution), node_modules, generated reports, screenshots.
tests/

# OS/editor junk that shows up everywhere, not just in tests/.
.DS_Store
Thumbs.db
